"use strict";

function calcAge(birthYear) {
  const age = 2022 - birthYear;
  console.log(firstName);
  function printAge(){
    console.log(`You are ${age} years old and born in ${birthYear}`);
  }
  printAge();
  return age;
}

var firstName = "Shiv";
console.log(calcAge(1987));
